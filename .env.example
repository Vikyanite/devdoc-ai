# 大模型 API 的 Key（必填）
LLM_API_KEY=your_api_key_here

# 大模型 API 的 Base URL
# 如果用 OpenAI 兼容的其他服务，就把地址改成对方提供的
LLM_BASE_URL=https://api.openai.com/v1

# 使用的模型名称
# 换成你实际要用的模型，例如 gpt-4.1-mini / deepseek-chat / qwen-turbo 等
LLM_MODEL=gpt-4.1-mini

# 建议是自己创建一个.env文件把上述的key都改为自己的